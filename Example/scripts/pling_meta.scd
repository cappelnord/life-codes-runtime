LCSpec(\pling, \meta, {|spec|
	spec.define((
		type: \pattern,
		color: Color.blue,
		quant: 4
	));

	spec.defineBlock(\pling, (
		type: \subject,
		primary: true
	));

	spec.defineBlock(\cycleSpeakers, (
		type: \modifier,
		primary: true
	));
});